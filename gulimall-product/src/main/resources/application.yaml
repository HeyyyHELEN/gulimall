spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://192.168.2.10:3312/gulimall_pms
    driver-class-name: com.mysql.jdbc.Driver
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
  application:
    name: gulimall-product
  thymeleaf:
    cache: false

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto

logging:
  level:
    cn.edu.hjnu: debug

server:
  port: 12000

